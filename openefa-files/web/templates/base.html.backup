<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}MailGuardian - Email Sentiment Analyzer{% endblock %}</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            padding-top: 20px;
        }
        .navbar {
            margin-bottom: 20px;
        }
        .sentiment-positive {
            color: green;
            font-weight: bold;
        }
        .sentiment-negative {
            color: red;
            font-weight: bold;
        }
        .sentiment-neutral {
            color: blue;
            font-weight: bold;
        }
        .card {
            margin-bottom: 20px;
        }
        .manipulation-high {
            color: red;
            font-weight: bold;
        }
        .manipulation-medium {
            color: orange;
            font-weight: bold;
        }
        .manipulation-low {
            color: green;
        }
        .category-spam, .category-phishing {
            color: red;
            font-weight: bold;
        }
        .entity {
            margin-right: 5px;
            padding: 2px 6px;
            border-radius: 4px;
            display: inline-block;
            margin-bottom: 5px;
        }
        .entity-money { background-color: #d4edda; }
        .entity-person { background-color: #d1ecf1; }
        .entity-org { background-color: #fff3cd; }
        .entity-location { background-color: #f8d7da; }
        .entity-date { background-color: #e2e3e5; }
        .filter-card {
            margin-bottom: 20px;
        }
        .pagination {
            margin-top: 20px;
            margin-bottom: 20px;
        }
        .nav-pills .nav-link.active {
            background-color: #0d6efd;
        }
    </style>
    {% block head %}{% endblock %}
</head>
<body>
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary rounded">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><i class="fas fa-chart-line"></i> MailGuardian - Email Sentiment Analyzer</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link {% if request.path == '/' %}active{% endif %}" href="/"><i class="fas fa-home"></i> Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {% if request.path == '/emails' %}active{% endif %}" href="/emails"><i class="fas fa-envelope"></i> Emails</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        {% block content %}{% endblock %}
    </div>

    <!-- Footer -->
    <footer class="bg-light text-center text-lg-start mt-4">
        <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.05);">
            © 2025 Email Sentiment Analyzer
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    {% block scripts %}{% endblock %}
</body>
</html>
